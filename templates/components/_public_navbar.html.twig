<nav class="navbar navbar-expand-lg home-navbar public-navbar sticky-top">
    <div class="container home-container">
        <a class="navbar-brand home-brand" href="{{ path('app_front_home') }}">
            <span class="home-brand__icon"><i data-lucide="sparkles"></i></span>
            <span class="home-brand__text"><span class="home-brand__art">Art</span><span class="home-brand__kids">Kids</span></span>
        </a>

        <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#publicMainNav" aria-controls="publicMainNav" aria-expanded="false" aria-label="Ouvrir le menu">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="publicMainNav">
            <ul class="navbar-nav mx-auto home-nav-links">
                <li class="nav-item"><a class="nav-link active" href="{{ path('app_front_home') ~ '#studio' }}">Studio</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_front_home') ~ '#modules' }}">Modules</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_front_home') ~ '#experience' }}">Experience</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_front_home') ~ '#pilotage' }}">Pilotage</a></li>
            </ul>

            <div class="home-nav-actions">
                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="btn home-btn home-btn-outline" href="{{ path('app_back_admin_index') }}">
                            <i data-lucide="layout-dashboard"></i>
                            Administration
                        </a>
                    {% elseif is_granted('ROLE_PARENT') %}
                        <a class="btn home-btn home-btn-outline" href="{{ path('app_front_parent_index') }}">
                            <i data-lucide="user-round"></i>
                            Mon espace
                        </a>
                    {% endif %}
                    <a class="btn home-btn home-btn-primary" href="{{ path('app_front_logout') }}">
                        <i data-lucide="log-out"></i>
                        Deconnexion
                    </a>
                {% else %}
                    <a class="btn home-btn home-btn-outline" href="{{ path('app_front_login') }}">
                        <i data-lucide="log-in"></i>
                        Se connecter
                    </a>
                    <a class="btn home-btn home-btn-primary" href="{{ path('app_front_register') }}">
                        <i data-lucide="user-plus"></i>
                        Creer un compte
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
